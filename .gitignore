# Environment variables — never commit your real .env file
.env
.env.local
.env.*.local

# Node dependencies
node_modules/

# Global state data (persisted at runtime, not tracked in git)
.data/

# macOS
.DS_Store
